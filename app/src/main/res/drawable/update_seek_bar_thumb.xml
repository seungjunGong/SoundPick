<?xml version="1.0" encoding="utf-8"?>
<!-- 원 모양의 시크바 컨트롤러 -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval"
    android:useLevel="false">
    <!-- 배경 -->
    <solid android:color="#ffffff" />
    <!-- 테두리 -->
    <stroke
        android:width="1dp"
        android:color="@color/base_seek_bar_color" />
    <!-- 크기 -->
    <size
        android:width="15dp"
        android:height="15dp"
        android:padding="5dp"/>
</shape>
