<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!--시크바 배경-->
    <item>
        <shape android:shape="line">
            <!--선 : 길이6dp -->
            <stroke android:width="6dp" android:color="@color/seek_bar_color"/>
        </shape>
    </item>
    <item>
        <clip>
            <shape android:shape="line">
                <stroke android:width="6dp" android:color="@color/base_seek_bar_color"/>
            </shape>
        </clip>
    </item>
    <!-- 시크바 프로그래스 -->
    <item>
        <clip>
            <shape android:shape="line">
                <stroke android:width="6dp" android:color="@color/base_seek_bar_color"/>
            </shape>
        </clip>
    </item>
</layer-list>
